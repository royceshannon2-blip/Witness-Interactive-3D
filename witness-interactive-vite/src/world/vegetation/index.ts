/**
 * Barrel for `world/vegetation/`.
 *
 * Per RENDERING.md §7.3 + CLAUDE.md "Thin Instances for repeated environmental
 * props": every plant species ships as one authored mesh + a thin-instance
 * matrix buffer scattered against a density map. Modules planned:
 *
 *   - Eucalyptus.ts      — eucalyptus trunks + leaf cards
 *   - Matooke.ts         — banana clumps
 *   - GrassTall.ts       — elephant grass cards
 *
 * None are implemented yet.
 */

export {};
