/**
 * Barrel for `world/props/`.
 *
 * Small narrative-relevant objects. v1 scope (per WORLD.md / MISSION_BLUEPRINT.md):
 *
 *   - Jerrycan.ts        — yellow plastic water container (Memory Fragment)
 *   - Bicycle.ts         — single bike, salvaged primitive pattern via thin instances
 *   - Lantern.ts         — kerosene lantern (cellar)
 *   - Letter.ts          — folded letter prop (opening)
 *
 * None are implemented yet. The prototype's primitive-tube helper
 * (`tube(from, to, radius, mat)`, audit §3) is salvageable and will land in
 * a `ProcPrimitives.ts` helper module here.
 */

export {};
