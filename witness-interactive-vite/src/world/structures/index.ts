/**
 * Barrel for `world/structures/`.
 *
 * Mud-brick + tin-roof archetypes used at Bisesero locations. Per
 * PROTOTYPE_AUDIT.md §5, the prototype's `createRugo` / `createShopfront` /
 * `createChurch` are NOT to be ported — those were Kigali-shaped. The
 * structures planned for v1 are:
 *
 *   - Rugo.ts            — traditional Rwandan compound (1994 / weathered 2026)
 *   - Cellar.ts          — sub-floor cellar interior
 *   - Dock.ts            — Lake Shore wooden dock
 *
 * None are implemented yet.
 */

export {};
